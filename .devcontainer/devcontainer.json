{
  "name": "Laravel 12 + Filament",
  "image": "mcr.microsoft.com/devcontainers/php:8.3",
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "xdebug.php-debug"
      ]
    }
  },
  "updateContentCommand": "sudo docker-php-ext-install intl && composer install && cp .env.example .env && php artisan key:generate",
  "postCreateCommand": "sudo docker-php-ext-enable intl && composer install && cp .env.example .env && php artisan key:generate",
  "forwardPorts": [8000]
}
