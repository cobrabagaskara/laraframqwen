{
  "name": "Laravel + Filament (Full PHP Extensions)",
  "image": "webdevops/php:8.3-cli",
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "xdebug.php-debug"
      ]
    }
  },
  "postCreateCommand": "composer install && cp .env.example .env && php artisan key:generate",
  "forwardPorts": [8000]
}
